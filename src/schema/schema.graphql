

type Query {
  helloWorld: String!
  users: [Users]
  user:Users
}

type Mutation {
  createUser(input: UserInput): Users
  updateUser(nickname:String!): UpdateUser
  login(input: UserLogin): String!
}
